{
  "name": "common",
  "version": "1.0.0",
  "description": "Shared code for CodeIngest client and server",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {}
}
