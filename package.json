{
  "name": "codeingest",
  "version": "1.0.0",
  "description": "GitHub code ingestion and analysis app",
  "main": "index.js",
  "scripts": {
    "client": "cd client && npm run dev",
    "server": "cd server && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install && cd ../common && npm install"
  },
  "keywords": [
    "github",
    "code",
    "analysis",
    "claude",
    "ai"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
